{
  "name": "homebrew-actions",
  "scripts": {
    "format": "prettier --write . '!.github/**/*.yml' !.rubocop.yml",
    "test": "node --test --import ./spec-helper.mjs --test-reporter ./test-reporter.mjs --test-reporter-destination stdout"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.1",
    "esm-reload": "^1.0.1",
    "js-yaml": "^4.1.1"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
